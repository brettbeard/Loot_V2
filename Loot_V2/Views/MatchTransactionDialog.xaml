<ContentDialog
    x:Class="Loot_V2.Views.MatchTransactionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    PrimaryButtonText="Match"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">860</x:Double>
    </ContentDialog.Resources>

    <Grid MinWidth="820" MinHeight="360">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="SubtitleText" Grid.Row="0"
                   Margin="0,0,0,8" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

        <controls:DataGrid
            Grid.Row="1"
            x:Name="CandidatesGrid"
            AutoGenerateColumns="False"
            GridLinesVisibility="Horizontal"
            SelectionMode="Single"
            SelectionChanged="OnGridSelectionChanged">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Date" Binding="{Binding DateDisplay}" Width="90"/>
                <controls:DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <controls:DataGridTextColumn Header="Type" Binding="{Binding TypeDisplay}" Width="90"/>
                <controls:DataGridTextColumn Header="Amount" Binding="{Binding AmountDisplay}" Width="100"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</ContentDialog>
